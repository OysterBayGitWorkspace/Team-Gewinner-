# Optional. If set, headless workers bill against the API key instead of the
# `claude login` session on this machine. Key lives in GCP Secret Manager,
# project ob-sourcing-brain, secret ANTHROPIC_API_KEY.
ANTHROPIC_API_KEY=
# Optional overrides
# PULSE_MODEL=claude-sonnet-5
# PULSE_MAX_WORKERS=8
